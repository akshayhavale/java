package com.example;

public class EmployeeSalaryCounter {
	
	
	
	 Map<Stiring, Double> salrySum = employees.stream().distanct().toMap(employee -> {
		employee.getDepartMent(), employee -> emp 
	 })
			 
			 
	select e.salary from Employee e order by desc limit 2 

}
